# Traço MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Web app estático mobile-first que gera overlay PNG transparente (rota + stats) estilo Strava para Instagram.

**Architecture:** SPA React + Vite + TS. Estado em átomos Jotai; posições em %. Lógica pura em `src/lib/*` (testável). UI em `src/components/*`. Export lê átomos e desenha em canvas offscreen 1080px.

**Tech Stack:** React 18, Vite 5, TypeScript, Jotai, Vitest + RTL + jsdom. Zero libs de mapa/canvas/GPX.

---

## File Structure

```
src/
  lib/geo.ts        haversine, normalizePoints (0-1, aspect preservado), elevationGain
  lib/gpx.ts        parseGpx(xml) -> { points, stats } via DOMParser
  lib/format.ts     formatDistance, formatDuration, formatPace
  lib/api.ts        searchPlaces (Nominatim), fetchRoute (OSRM)
  lib/export.ts     renderOverlay(state, size) -> Promise<Blob>
  state.ts          átomos: preset, elements, route, style
  fonts.ts          OVERLAY_FONTS const
  components/App.tsx
  components/EditorCanvas.tsx   proporção do preset, xadrez, drag
  components/RouteLayer.tsx     SVG polyline, anima stroke-dashoffset
  components/StatBlock.tsx      label+valor editável
  components/StylePanel.tsx     cores, fonte, espessura
  components/RouteSource.tsx    tabs GPX | Buscar local | Início e fim
  components/ExportButton.tsx   share/download
  styles.css        tokens do design (frontend-design)
test/
  geo.test.ts  gpx.test.ts  format.test.ts  app.test.tsx
```

### Task 1: Scaffold

- [ ] `npm create` manual: package.json (react, react-dom, jotai; dev: vite, @vitejs/plugin-react, typescript, vitest, jsdom, @testing-library/react, @testing-library/user-event), tsconfig, vite.config.ts (test: environment jsdom), index.html
- [ ] `npm install` — Expected: sem erros
- [ ] Commit `chore: scaffold vite react ts`

### Task 2: geo.ts (TDD)

- [ ] Failing test `test/geo.test.ts`:

```ts
import { haversine, normalizePoints, elevationGain } from '../src/lib/geo'

it('haversine BH->SP ~490km', () => {
  const d = haversine({ lat: -19.9167, lon: -43.9345 }, { lat: -23.5505, lon: -46.6333 })
  expect(d).toBeGreaterThan(480_000); expect(d).toBeLessThan(510_000)
})
it('normalize keeps aspect and fits 0-1', () => {
  const pts = normalizePoints([{ lat: 0, lon: 0 }, { lat: 1, lon: 1 }, { lat: 0, lon: 1 }])
  for (const p of pts) { expect(p.x).toBeGreaterThanOrEqual(0); expect(p.x).toBeLessThanOrEqual(1); expect(p.y).toBeGreaterThanOrEqual(0); expect(p.y).toBeLessThanOrEqual(1) }
  expect(pts[0].y).toBeGreaterThan(pts[1].y) // lat maior = y menor (norte pra cima)
})
it('elevationGain soma só deltas positivos', () => {
  expect(elevationGain([600, 650, 620, 700])).toBe(130)
})
```

- [ ] Run `npx vitest run test/geo.test.ts` — Expected: FAIL (module não existe)
- [ ] Implement `src/lib/geo.ts`:

```ts
export type LatLon = { lat: number; lon: number; ele?: number; time?: number }
export type Pt = { x: number; y: number }

export function haversine(a: LatLon, b: LatLon): number {
  const R = 6371000, toRad = (d: number) => (d * Math.PI) / 180
  const dLat = toRad(b.lat - a.lat), dLon = toRad(b.lon - a.lon)
  const h = Math.sin(dLat / 2) ** 2 + Math.cos(toRad(a.lat)) * Math.cos(toRad(b.lat)) * Math.sin(dLon / 2) ** 2
  return 2 * R * Math.asin(Math.sqrt(h))
}

export function normalizePoints(pts: LatLon[]): Pt[] {
  const lats = pts.map(p => p.lat), lons = pts.map(p => p.lon)
  const minLat = Math.min(...lats), maxLat = Math.max(...lats)
  const minLon = Math.min(...lons), maxLon = Math.max(...lons)
  const midLat = (minLat + maxLat) / 2
  const w = (maxLon - minLon) * Math.cos((midLat * Math.PI) / 180), h = maxLat - minLat
  const scale = Math.max(w, h) || 1
  return pts.map(p => ({
    x: (((p.lon - minLon) * Math.cos((midLat * Math.PI) / 180)) / scale) + (1 - w / scale) / 2,
    y: ((maxLat - p.lat) / scale) + (1 - h / scale) / 2,
  }))
}

export function elevationGain(eles: number[]): number {
  let g = 0
  for (let i = 1; i < eles.length; i++) if (eles[i] > eles[i - 1]) g += eles[i] - eles[i - 1]
  return g
}

export function totalDistance(pts: LatLon[]): number {
  let d = 0
  for (let i = 1; i < pts.length; i++) d += haversine(pts[i - 1], pts[i])
  return d
}
```

- [ ] Run — Expected: PASS. Commit `feat: geo utils`

### Task 3: gpx.ts (TDD)

- [ ] Failing test com GPX mínimo (3 trkpt com ele+time), asserta points.length 3, stats.distanceM > 0, stats.durationS = diferença timestamps, stats.gainM correto; GPX sem trkpt lança `Error('GPX sem trackpoints')`
- [ ] Implement: DOMParser, `querySelectorAll('trkpt')`, lê `ele`/`time` filhos; stats = { distanceM, durationS?, gainM? }; pace derivado no consumer
- [ ] Run PASS. Commit `feat: gpx parser`

### Task 4: format.ts (TDD)

- [ ] Tests: `formatDistance(4850)='4,85 km'`; `formatDuration(4380)='1h 13m'`; `formatDuration(300)='5m'`; `formatPace(4850, 4380)="15'03\"/km"`
- [ ] Implement com Intl.NumberFormat('pt-BR'). Run PASS. Commit `feat: formatters`

### Task 5: api.ts

- [ ] `searchPlaces(q)`: GET `https://nominatim.openstreetmap.org/search?format=json&limit=5&q=`; header via query só (browser não permite User-Agent custom — usar param `email` opcional); retorna `{ name, lat, lon }[]`
- [ ] `fetchRoute(a, b)`: GET `https://router.project-osrm.org/route/v1/foot/{lonA},{latA};{lonB},{latB}?overview=full&geometries=geojson`; retorna LatLon[] + distanceM + durationS
- [ ] Sem teste unitário (só fetch); erro de rede vira mensagem na UI. Commit `feat: nominatim+osrm clients`

### Task 6: state.ts + fonts.ts

- [ ] Átomos: `presetAtom ('story'|'feed')`, `routeAtom (Pt[]|null)`, `styleAtom { routeColor, textColor, font, strokeWidth }`, `elementsAtom` = array `{ id, label, value, x, y }` (x,y em %; rota também tem x,y,size %: `routeBoxAtom { x, y, size }`)
- [ ] `OVERLAY_FONTS = ['Archivo Black','Bebas Neue','Space Mono','Lora','Caveat']`, carregadas no index.html via Google Fonts
- [ ] Commit `feat: state atoms`

### Task 7: export.ts

- [ ] `renderOverlay({ elements, route, routeBox, style }, {w,h})`: OffscreenCanvas ou canvas DOM; `await document.fonts.load` por fonte usada; desenha polyline (lineJoin round, strokeWidth % da largura) e textos (label menor acima, valor grande abaixo, mesma proporção do editor: font-size % da largura); `canvas.convertToBlob?.() ?? toBlob` PNG
- [ ] Teste manual (jsdom sem canvas real). Commit `feat: png export`

### Task 8: UI (App, EditorCanvas, RouteLayer, StatBlock, StylePanel, RouteSource, ExportButton, styles.css)

- [ ] styles.css: tokens `--paper #EDEFEC --ink #17201B --accent #FF4D12 --contour #C9CFC7`; Archivo UI; xadrez via repeating-conic-gradient; bottom sheets; contour SVG backdrop; safe-areas mobile
- [ ] EditorCanvas: div com aspect-ratio do preset; drag = onPointerDown captura, move converte deltas pra %, clamp 0-100; elemento rota é caixa arrastável+redimensionável (handle canto)
- [ ] RouteLayer: `<svg viewBox="0 0 100 100" preserveAspectRatio>` polyline de route Pt[]; animação stroke-dashoffset 1x ao montar (respeita prefers-reduced-motion)
- [ ] StatBlock: contentEditable simples label+valor, botão remover; botão "+ stat" no painel
- [ ] RouteSource: 3 tabs; GPX input file -> parseGpx -> seta routeAtom + pré-preenche elements (Distância, Tempo, Elevação, Pace) se vazios; Buscar local -> lista resultados -> por ora centra mapa? NÃO (YAGNI): busca local retorna 1 ponto — sem rota; então tab "Buscar local" vira busca de início E fim? Decisão: tab 2 = busca 1 local e usa como início com fim igual (não gera traço) — inútil. Corrigir design: tabs = "Arquivo GPX" | "Início e fim" (cada campo com autocomplete Nominatim; rota OSRM). 2 tabs. Spec dizia 3; "buscar local" só fazia sentido como açúcar do início-fim. Registrar no STATE.
- [ ] ExportButton: gera blob nos 2 presets sob demanda; `navigator.share({files})` se suportado senão `<a download>`
- [ ] app.test.tsx: renderiza App; adiciona StatBlock via botão; remove; troca preset e asserta elemento continua com mesmo style.left %
- [ ] Run all tests PASS. Commit `feat: editor ui`

### Task 9: Build + verificação

- [ ] `npx vitest run` all PASS
- [ ] `npx tsc --noEmit` sem erros
- [ ] `npx vite build` sem erros
- [ ] Commit final, atualizar STATE.md (pendências ≤3)

## Self-review

- Spec coverage: rota GPX ✓ (T3), início-fim ✓ (T5/T8), stats manuais+pré-preenchidos ✓ (T8), personalização ✓ (T6/T8), export 2 presets ✓ (T7/T8), mobile-first ✓ (T8), stateless ✓. Gap encontrado e corrigido: tab "buscar local" isolada não produz rota — colapsada em "Início e fim" com autocomplete.
- Placeholders: nenhum TBD.
- Consistência de tipos: LatLon/Pt definidos em T2, usados em T3/T5/T6/T7.
